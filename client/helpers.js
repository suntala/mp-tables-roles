/*
import { HTTP } from 'meteor/http'
import moment from 'moment';

Template.registerHelper('groupcolumns', function() {
    return [
        {
            id: 'columnName',
            data: 'name',
            title: 'Column Name',
            render: function(val, type, doc) {
              if (val) {
                return 'val'
              } else {
                return '';
              }
            }
        },
        {
            id: 'columnDate',
            data: 'createdAt',
            title: 'Column Date',
            render: function(val, type, doc) {
              if (val) {
                return 'val'
              } else {
                return '';
              }
            }
        },
    ];
});
*/